/**
 * Preload script — runs in renderer before web content loads.
 * Use contextBridge to expose safe APIs to the web app.
 *
 * Future: expose shell.showItemInFolder() for "Open in Finder" button.
 */

// Placeholder — no APIs exposed yet
export {};
